<UserControl
    x:Class="OmegaGo.UI.WindowsUniversal.UserControls.TsumegoPuzzleStateControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmegaGo.UI.WindowsUniversal.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="{x:Bind Background, Mode=OneWay}">
        <StackPanel Orientation="Horizontal" Height="48" VerticalAlignment="Top">
            <FontIcon Glyph="{x:Bind Glyph, Mode=OneWay}" Width="48" Height="48" Margin="0,0,8,0" />
            <TextBlock VerticalAlignment="Center" Text="{x:Bind Text, Mode=OneWay}" />
        </StackPanel>
    </Border>
</UserControl>
