<UserControl
    x:Class="OmegaGo.UI.WindowsUniversal.UserControls.PlayerPortrait"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmegaGo.UI.WindowsUniversal.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border Background="{x:Bind Color}">
        <Border Background="White" BorderBrush="Black" BorderThickness="1" Margin="10">
            <StackPanel>
                <TextBlock HorizontalAlignment="Center" Text="{x:Bind ViewModel.Name}"></TextBlock>
                <TextBlock HorizontalAlignment="Center" Text="Prisoners: (uncounted)"></TextBlock>
                <TextBlock HorizontalAlignment="Center" FontSize="26" TextWrapping="Wrap" Text="{x:Bind ViewModel.TimeControlMainLine, Mode=OneWay}"></TextBlock>
                <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="{x:Bind ViewModel.TimeControlSubLine, Mode=OneWay}"></TextBlock>
            </StackPanel>
        </Border>
    </Border>
</UserControl>
