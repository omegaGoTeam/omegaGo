<local:UserControlBase
    x:Class="OmegaGo.UI.WindowsUniversal.UserControls.BoardSettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmegaGo.UI.WindowsUniversal.UserControls"
    xmlns:game="using:OmegaGo.UI.Services.Game"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{x:Bind Path=BoardControlState}"
    d:DataContext="{d:DesignInstance game:BoardControlState, IsDesignTimeCreatable=True}"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Padding="15">
        <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
            <StackPanel>
                <TextBlock Text="{x:Bind Localizer.BoardSettings}" HorizontalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="{x:Bind Localizer.CellSize}" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <Slider Value="{Binding Path=CellSize, Mode=TwoWay}" />
                <TextBlock Text="{x:Bind Localizer.BoardBorderThickness}" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <Slider Value="{Binding Path=BoardBorderThickness, Mode=TwoWay}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</local:UserControlBase>
