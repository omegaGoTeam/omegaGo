<views:TransparencyViewBase
    x:Class="OmegaGo.UI.WindowsUniversal.Views.KgsHomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:OmegaGo.UI.WindowsUniversal.Views"
    xmlns:userControls="using:OmegaGo.UI.WindowsUniversal.UserControls"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo />
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Pivot Grid.Row="0">
            <PivotItem Header="Games">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Row="0">
                        <TextBlock VerticalAlignment="Center" Margin="0,0,10,0">Show:</TextBlock>
                        <ComboBox SelectedIndex="0">
                            <ComboBoxItem>All games in progress</ComboBoxItem>
                            <ComboBoxItem>All challenges</ComboBoxItem>
                            <ComboBoxItem>Joined Room 1</ComboBoxItem>
                            <ComboBoxItem>Joined Room 2</ComboBoxItem>
                            <ComboBoxItem>Joined Room 3</ComboBoxItem>
                        </ComboBox>
                        <CheckBox>Show robots</CheckBox>
                    </StackPanel>
                    <ListBox Grid.Row="1">
                        
                    </ListBox>
                    <StackPanel Grid.Row="2">
                        <Button>Join</Button>
                    </StackPanel>
                </Grid>
            </PivotItem>
            <PivotItem Header="Rooms">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <TextBlock>All rooms: </TextBlock>
                        <ListBox Grid.Row="1" Width="200"></ListBox>
                    </Grid>
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Grid.Row="0">
                            <Button>Join/unjoin</Button>
                        </StackPanel>
                        <TextBox Grid.Row="1" Height="300" AcceptsReturn="True" TextWrapping="Wrap" Text="description">
                        </TextBox>
                        <TextBlock Grid.Row="2">Games and challenges:</TextBlock>
                        <ListBox Grid.Row="3"></ListBox>
                    </Grid>
                </Grid>
            </PivotItem>
            <PivotItem Header="Automatch">
                <TextBlock>To be done.</TextBlock>
            </PivotItem>
        </Pivot>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Command="{x:Bind VM.LogoutCommand}">Logout</Button>
            <Button>Manual refresh</Button>
        </StackPanel>
        <userControls:LoginFormControl ViewModel="{x:Bind VM.LoginForm}" />
    </Grid>
</views:TransparencyViewBase>

