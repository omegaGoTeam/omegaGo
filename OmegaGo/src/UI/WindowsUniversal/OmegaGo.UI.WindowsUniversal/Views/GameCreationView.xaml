<views:ViewBase
    x:Class="OmegaGo.UI.WindowsUniversal.Views.GameCreationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:OmegaGo.UI.WindowsUniversal.Views"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo />
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Game Creation" 
                   HorizontalAlignment="Center"
                   Margin="0,20,0,0"
                   Grid.Row="0"
                   Style="{ThemeResource HeaderTextBlockStyle}" />

        <Grid x:Name="GameSettingsGrid"
              HorizontalAlignment="Center"
              Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Board Size" Grid.Column="0" Grid.Row="0" 
                       HorizontalAlignment="Right" VerticalAlignment="Center" 
                       FontSize="22" Margin="5" />
            <ComboBox Grid.Column="1" Grid.Row="0" Margin="5" Width="125" />

            <TextBlock Text="Rules" Grid.Column="0" Grid.Row="1" 
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Center" 
                       FontSize="22" Margin="5" />
            <ComboBox Grid.Column="1" Grid.Row="1" Margin="5" HorizontalAlignment="Stretch" />

            <TextBlock Text="Your Color" Grid.Column="0" Grid.Row="2" 
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Center" 
                       FontSize="22" Margin="5" />
            <ComboBox Grid.Column="1" Grid.Row="2" Margin="5" HorizontalAlignment="Stretch" />

            <TextBlock Text="Difficulty" Grid.Column="0" Grid.Row="3" 
                       HorizontalAlignment="Right" VerticalAlignment="Center" 
                       FontSize="22" Margin="5" />
            <ComboBox Grid.Column="1" Grid.Row="3" Margin="5" HorizontalAlignment="Stretch" />

            <TextBlock Text="White Handicap" Grid.Column="0" Grid.Row="4" 
                       HorizontalAlignment="Right" VerticalAlignment="Center" 
                       FontSize="22" Margin="5" />
            <TextBox Grid.Column="1" Grid.Row="4" Margin="5" />

            <Border Grid.ColumnSpan="2" Grid.Row="5" 
                    Margin="15"
                    BorderBrush="Gray" BorderThickness="1" />

        </Grid>

        <Button Grid.Row="2" 
                Content="Play"
                Command="{Binding Path=NavigateToGame}"
                HorizontalAlignment="Center"/>
    </Grid>
</views:ViewBase>
