<views:ViewBase
    x:Class="OmegaGo.UI.WindowsUniversal.Views.HelpView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:OmegaGo.UI.WindowsUniversal.Views"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo />
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0">Topics: </TextBlock>
            <ScrollViewer Grid.Row="1" >
            <ListBox Width="350" ScrollViewer.HorizontalScrollMode="Enabled"
                     
                     ScrollViewer.HorizontalScrollBarVisibility="Visible">
                <ListBox.Items>
                    <TextBlock>Welcome to Go!</TextBlock>
                    <TextBlock>Comprehensive Rules</TextBlock>
                </ListBox.Items>
                </ListBox>
            </ScrollViewer>
            <Grid Padding="5" Grid.Row="2">
                <Button  Content="Back"  Click="GoBack_Click" />
            </Grid>
        </Grid>
        <TextBox TextWrapping="Wrap" Grid.Column="1" Text="This will be a Rich Text Box in the future.">
            
        </TextBox>
    </Grid>
</views:ViewBase>
