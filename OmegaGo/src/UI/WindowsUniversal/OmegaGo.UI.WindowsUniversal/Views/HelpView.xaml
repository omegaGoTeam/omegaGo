<views:TransparencyViewBase
    x:Class="OmegaGo.UI.WindowsUniversal.Views.HelpView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:OmegaGo.UI.WindowsUniversal.Views"
    xmlns:navigation="using:OmegaGo.UI.WindowsUniversal.UserControls.Navigation"
    Loaded="TransparencyViewBase_Loaded"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo />
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <SplitView IsPaneOpen="True"
               Name="HelpSplitView"
               DisplayMode="CompactInline">
        <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button x:Name="HamburgerButton" Grid.Row="0" Click="OpenCloseHelp">
                    <FontIcon Glyph="&#xE700;"></FontIcon>
                </Button>
                <navigation:TitleControl Text="{x:Bind Localizer.Help}" Grid.Row="1" />
                <ScrollViewer Grid.Row="2">
                    <ListBox ScrollViewer.HorizontalScrollMode="Enabled"
                     ItemsSource="{Binding HelpItems}"
                     SelectedItem="{Binding SelectedHelpItem, Mode=TwoWay}"
                     ScrollViewer.HorizontalScrollBarVisibility="Visible">
                    </ListBox>
                </ScrollViewer>
            </Grid>
        </SplitView.Pane>
        <Border BorderBrush="Black" BorderThickness="2">
            <WebView DefaultBackgroundColor="White" Name="WebView">

            </WebView>
        </Border>
    </SplitView>
</views:TransparencyViewBase>
