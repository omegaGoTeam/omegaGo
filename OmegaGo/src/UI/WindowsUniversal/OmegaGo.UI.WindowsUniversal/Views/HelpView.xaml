<views:TransparencyViewBase
    x:Class="OmegaGo.UI.WindowsUniversal.Views.HelpView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:OmegaGo.UI.WindowsUniversal.Views"
    Loaded="TransparencyViewBase_Loaded"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo />
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0">Topics: </TextBlock>
            <ScrollViewer Grid.Row="1" >
                <ListBox Width="350" ScrollViewer.HorizontalScrollMode="Enabled"
                     ItemsSource="{Binding HelpItems}"
                     SelectedItem="{Binding SelectedHelpItem, Mode=TwoWay}"
                     ScrollViewer.HorizontalScrollBarVisibility="Visible">
                </ListBox>
            </ScrollViewer>
                <Button Grid.Row="2"  Content="Back" HorizontalContentAlignment="Stretch" Click="GoBack_Click" />
        </Grid>
        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="2">
        <WebView DefaultBackgroundColor="Transparent" Name="WebView">

            </WebView>
        </Border>
    </Grid>
</views:TransparencyViewBase>
