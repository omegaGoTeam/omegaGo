<views:TransparencyViewBase
    x:Class="OmegaGo.UI.WindowsUniversal.Views.SingleplayerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:OmegaGo.UI.WindowsUniversal.Views"
    xmlns:quests="using:OmegaGo.UI.Services.Quests"
    xmlns:navigation="using:OmegaGo.UI.WindowsUniversal.UserControls.Navigation"
    x:Name="SingleplayerViewLayout"
    Loaded="TransparencyViewBase_Loaded"
    mc:Ignorable="d">


    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo />
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0,20,0,0"></Setter>
                    <Setter Property="MaxWidth" Value="300"></Setter>
                    <Setter Property="HorizontalAlignment" Value="Stretch"></Setter>
                </Style>
            </StackPanel.Resources>
                <navigation:TitleControl Text="{x:Bind Localizer.SingleplayerMenu}" HorizontalAlignment="Center"></navigation:TitleControl>

                <Button Content="{x:Bind Localizer.Tutorial}" Command="{Binding Path=GoToTutorial}"></Button>
                <Button Content="{x:Bind Localizer.SolvePuzzles}" Command="{Binding Path=GoToTsumegoMenu}"></Button>
                <Button Content="{x:Bind Localizer.PlayAgainstAI}" Command="{Binding Path=GoToLocalGame}"></Button>
                <Button Content="{x:Bind Localizer.Statistics}" Command="{Binding Path=GoToStatistics}"></Button>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="{x:Bind Localizer.PointsColon}" FontSize="26" Margin="0,0,10,0"></TextBlock>
                <TextBlock Text="{Binding Points}" FontSize="26" FontWeight="Bold"></TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="{x:Bind Localizer.OmegaGoRank}" FontSize="26" Margin="0,0,10,0"></TextBlock>
                <TextBlock Text="{Binding Rank}" FontSize="26" FontWeight="Bold"></TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{x:Bind Localizer.NextRankAtColon}" FontSize="26" Margin="0,0,10,0"></TextBlock>
                    <TextBlock Text="{Binding NextRankPoints}" FontSize="26" FontWeight="Bold"></TextBlock>
                </StackPanel>
                <TextBlock FontSize="26"><!-- SPACER --></TextBlock>
            <TextBlock Text="{x:Bind Localizer.Quests}" HorizontalAlignment="Center" FontSize="26"></TextBlock>
            <GridView SelectionMode="None" IsItemClickEnabled="True" HorizontalAlignment="Center" ItemsSource="{Binding ActiveQuests}">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="quests:ActiveQuest">
                        <Border Background="Wheat" BorderBrush="Blue" BorderThickness="1" Width="300" Height="250">
                                <StackPanel>
                                    <TextBlock TextAlignment="Center" Text="{Binding Quest.Name}"  Foreground="Black" FontSize="28" TextWrapping="Wrap" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock  FontSize="22" Margin="0,0,10,0" >Reward: </TextBlock>
                                    <TextBlock Text="{Binding Quest.PointReward}"
                                        FontWeight="Bold"   Foreground="Black" FontSize="22" />
                                </StackPanel>
                                <TextBlock Text="{Binding Quest.Description}" Padding="0,10,0,0" TextAlignment="Center"
                                           FontSize="18" TextWrapping="Wrap" />
                                <ProgressBar Minimum="0" Maximum="{Binding Quest.MaximumProgress}" Value="{Binding Progress}" Height="20" Margin="0,10,0,0" >
                                </ProgressBar>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Progress}"></TextBlock>
                                    <TextBlock Text="/"></TextBlock>
                                    <TextBlock Text="{Binding Quest.MaximumProgress}"></TextBlock>
                                </StackPanel>
                                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                        <Button Visibility="{Binding Quest.TryThisNowButtonVisible, Converter={StaticResource VisibilityConverter}}" Tag="{Binding}"
                                                Click="TryThisNow_Click" ToolTipService.ToolTip="{Binding ElementName=SingleplayerViewLayout, Path=Localizer.Quest_TryThisNowTooltip}">
                                            <StackPanel Orientation="Horizontal">
                                                <FontIcon Glyph="&#xE768;" Margin="0,0,10,0"></FontIcon>
                                                <TextBlock Text="{Binding ElementName=SingleplayerViewLayout, Path=Localizer.Quest_TryThisNow}"></TextBlock>
                                            </StackPanel>
                                        </Button>
                                        <Button Visibility="{Binding ElementName=SingleplayerViewLayout, Path=VM.ExchangeIsPossible, Converter={StaticResource VisibilityConverter}, Mode=OneWay}" Tag="{Binding}"
                                                Click="ExchangeQuest_Click" ToolTipService.ToolTip="{Binding ElementName=SingleplayerViewLayout, Path=Localizer.Quest_ReplaceTooltip}">
                                            <StackPanel Orientation="Horizontal">
                                                <FontIcon Glyph="&#xE72C;" Margin="0,0,10,0"></FontIcon>
                                                <TextBlock Text="{Binding ElementName=SingleplayerViewLayout, Path=Localizer.Quest_Replace}">Replace</TextBlock>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                        </Border>
                    </DataTemplate>
                </GridView.ItemTemplate>
                    <!-- A grid view maybe won't work... we need larger LastViewItems -->
                    <quests:ExampleActiveQuest></quests:ExampleActiveQuest>
                    <quests:ExampleActiveQuest></quests:ExampleActiveQuest>
                </GridView>
            <TextBlock Text="{x:Bind Localizer.QuestsFooter}" HorizontalAlignment="Center" Margin="0,0,0,20"></TextBlock>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</views:TransparencyViewBase>
